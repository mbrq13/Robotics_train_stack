# Station endpoint.
station_url: ws://192.168.55.100:8765
# Task instruction for language-conditioned policies.
task: ""
device: cuda
schema_version: 1
# Representation expected by the checkpoint and hardware station profile.
action_space: normalized_100
# Use `rtc` only with an RTC-trained checkpoint.
execution_mode: standard
rtc_execution_horizon: 10
rtc_refill_threshold: 10
# Explicit state ordering for checkpoints that omit semantic names.
state_names:
  - left_joint_1
  - left_joint_2
  - left_joint_3
  - left_joint_4
  - left_joint_5
  - left_joint_6
  - left_gripper
  - right_joint_1
  - right_joint_2
  - right_joint_3
  - right_joint_4
  - right_joint_5
  - right_joint_6
  - right_gripper
